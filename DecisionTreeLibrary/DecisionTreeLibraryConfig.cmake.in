get_filename_component(DecisionTree_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

if(NOT TARGET DecisionTreeLibrary::DecisionTree)
    include("${DecisionTree_CMAKE_DIR}/DecisionTreeLibraryTarget.cmake")
endif()